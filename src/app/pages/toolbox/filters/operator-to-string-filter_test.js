/* global describe, beforeEach, it, expect, inject, module */
'use strict';

describe('operatorToString', function () {
  beforeEach(module('airlst.toolbox'));
  //
  //it('should filter our numbers not greater than 3', inject(function ($filter) {
  //  expect($filter('operatorToString')([1, 2, 3, 4])).to.include.members([4]);
  //}));
});
